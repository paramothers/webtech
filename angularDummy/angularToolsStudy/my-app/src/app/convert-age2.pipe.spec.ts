import { ConvertAge2Pipe } from './convert-age2.pipe';

describe('ConvertAge2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertAge2Pipe();
    expect(pipe).toBeTruthy();
  });
});
