let message = [
    'first message',
        'second message',
            'third message'
            ]
            
message[0]
message[5]
message.forEach(e => console.log(e))
message.slice(2,3)
message.splice(2,1)
message.forEach(e => console.log(e))
