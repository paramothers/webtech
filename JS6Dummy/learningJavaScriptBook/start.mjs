import {outerFunction,secondFunction,closureDemo} from './functional/inner';
import {tap} from './functional/sortby';
import {exploreMath} from './Math/mymath';

// outerFunction("paramasivam");
// secondFunction();

// const fun = closureDemo(123);
// fun();

// [1,2,3,4].forEach(
//     (number) => {
//         tap(number)(() => {console.log(number)})
//     }
// )
// const returnedFunction = tap("param");
// returnedFunction(secondFunction);

// [1,2,4,5,5].map( (a) => {return a*a})

// console.log(` hai,
// param, sivam`);

// console.log("hai param \n how are you ?");

// console.log("type of NAN is", typeof(NaN));

// console.log(Math.PI);
exploreMath();

